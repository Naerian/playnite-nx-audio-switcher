<playnite:PluginUserControl x:Class="PlayniteAudioSwitcher.AudioBatteryTopPanelControl"
                            x:Name="Root"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:playnite="clr-namespace:Playnite.SDK.Controls;assembly=Playnite.SDK"
                            Focusable="False">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <StackPanel.ToolTip>
            <TextBlock Text="{Binding CurrentDeviceName}" />
        </StackPanel.ToolTip>
        <Viewbox Width="20" Height="20" Margin="2,0,5,0">
            <Viewbox.Style>
                <Style TargetType="Viewbox">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ShowDesktopIndicatorIcon}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Viewbox.Style>
            <Path Data="{Binding DesktopIndicatorIconGeometry}"
                  Stretch="Uniform"
                  Stroke="{Binding Foreground, ElementName=Root}"
                  StrokeThickness="2"
                  StrokeStartLineCap="Round"
                  StrokeEndLineCap="Round"
                  StrokeLineJoin="Round"
                  Fill="Transparent" />
        </Viewbox>
        <TextBlock Text="{Binding CurrentDeviceBatteryLabel}"
                   Foreground="{Binding Foreground, ElementName=Root}"
                   VerticalAlignment="Center"
                   FontWeight="SemiBold"
                   Margin="0,0,2,0">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding HasCurrentDeviceBattery}" Value="True" />
                                <Condition Binding="{Binding ShowDesktopIndicatorPercentage}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </StackPanel>
</playnite:PluginUserControl>
